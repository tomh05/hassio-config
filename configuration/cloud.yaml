google_actions:
  filter:
    include_entities:

      - script.cinema_mode

      - input_boolean.holiday_mode

  entity_config:
    script.cinema_mode:
      name: Cinema Mode
      room: Living Room
